image: gitpod/workspace-full-vnc

ports:
  - port: 6080

tasks:
  - init: sudo apt-get update & sudo apt-get install -y firefox & wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb & sudo apt install -y ./google-chrome-stable_current_amd64.deb
